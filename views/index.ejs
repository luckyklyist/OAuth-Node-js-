<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/tailwind.css">
  <title>Home</title>
</head>

<body class="">
  <%- include('nav')%>
    <form action="" method="post">
      <div class="relative flex-grow w-full container px-5 pt-24 pb-10 mx-auto">
        <label for="showName" class="leading-7 text-sm text-gray-600">Name of the shows</label>
        <div class="flex">
          <input type="text" id="showName" name="showName"
            class="w-full bg-gray-100 bg-opacity-50 rounded border border-gray-300 focus:border-indigo-500 focus:bg-transparent focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out"
            value="<%=showName%>">
          <button class="bg-black p-2 rounded-sm text-white hover:scale-110 hover:ease-linear ml-3">Search</button>
        </div>
      </div>

    </form>
    <%if (scraped) {%>
      <div class="mx-10 p-2">
        <div class="text-center font-bold text-gray-700 text-xl mb-2">
          <%= seriesName %>
        </div>
        <div>
          <div class="grid grid-cols-1 gap-4 gap-y-6 border-b border-gray-300 py-2 pb-20 md:grid-cols-2 lg:grid-cols-4">
            <% for(let i=0; i < charName.length; i++) { %>
              <div class="rounded-md border overflow-hidden shadow-lg hover:shadow-xl">
                <img src="<%=scrapeImg[i]%>" alt="Gabrielle Fernandez" class="object-contain h-40 w-full" />
                <div class="p-4">
                  <p class="text-xl font-semibold text-gray-900 mb-2">
                    <%= charName[i] %>
                  </p>
                  <p class="text-sm font-semibold text-gray-500 mb-4">
                    <%=castName[i]%>
                  </p>
                  <button type="button"
                    class="text-white bg-gradient-to-r from-green-500 via-green-600 to-green-700 hover:bg-green-800 focus:ring-4 focus:ring-green-300 dark:focus:ring-green-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center mx-auto mb-4 hover:scale-105 transition duration-300">
                    <a
                      href="/characters/add?characterName=<%= charName[i] %>&castName=<%=castName[i]%>&favCharUrl=<%=scrapeImg[i]%>&showName=<%=showName%>">
                      Add
                    </a>
                  </button>
                  <a href="<%=charDetail[i]%>">
                    Detail
                  </a>
                </div>
              </div>
              <% } %>
          </div>
        </div>
      </div>


      <%}%>
        </div>

        <% if (error) { %>
          <div class="bg-orange-100 border-l-4 border-orange-500 text-orange-700 p-4 m-10" role="alert">
            <p class="font-bold">Error fetching data</p>
            <p>Please check the show name and try again.</p>
          </div>
          <% } %>
            <% if (!authenticated) { %>
              <div
                class="mx-44 text-justify bg-gradient-to-b from-gray-900 to-black bg-opacity-70 backdrop-filter backdrop-blur-lg rounded-lg p-8 ">
                <div class="text-4xl text-center font-bold text-white my-8">
                  Welcome to the HBO Cast Scraper! 🎉
                </div>
                <p class="text-lg text-white">
                  Write the names of your favorite TV shows and see the names of the cast members. Add them to your
                  profile as your
                  favorite cast with the characters they portray.
                </p>
                <p class="text-lg mt-6 text-white">
                  I've already added my list with <span class="text-green-400">Zendaya</span> from Euphoria 🎭,
                  <span class="text-green-400">Pedro Pascal</span> from The Last of Us 🎮, and
                  <span class="text-green-400">Maisie Williams</span> from Game of Thrones 🐉.
                </p>
                <p class="text-lg mt-6 text-white">
                  What did you add? Let's get started! Login with GitHub to begin the exciting journey! 🔐
                </p>
              </div>
              <% } %>
                <%- include('footer')%>
</body>

</html>